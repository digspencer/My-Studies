console.log("a =", a)
var a = 2;
console.log("a =", a)

// Em JS acontece o Hoisting, ou seja, a variável é "jogada" para cima ainda que seja declarado embaixo. (içamento), só ocorre se a variável estiver declarada em VAR
// não tem motivo para fazer isso, então comece declarando suas variáveis antes de usá-las